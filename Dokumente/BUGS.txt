Allgemein : Algorithmen sind noch sehr beta, die hab ich pausiert seit der Umfang feststand (und die nicht dazugehörten).
Kommentare in 
() = Problem behoben bzw. erklärt
<-- = bisher keine Lösung bekannt
!!() = nicht reproduzierbar
 
Bugs:
-Blendet man ein Raster ein und die Knoten sollen ausgerichtet werden, sind es aber noch nicht und mkiert man dann einen Teil und klickt die rechte Maustaste gibts einen Zeichenfehler, wodurch auch nicht mehr das richtige Menü kommt, weil der Knoten intern wohl schon anders angeordnet ist
!!(bisher hier nicht reproduzierbar)
-Markiert man in einem Fenster einen Knoten/Kante könnte man es auch im anderen Fenster makieren
(hm, muss ich nochmal sehen wie und ob das so einfach umsetzbar ist, ist aber Algorithmen sind ja BETA)
-Man sollte Knoten nicht übereinanderlegen können. Ist gerade bei Raster mit Snapping sehr einfach, aber kann einen verwirren
(hm, ich weiß nich man hat ja im Kontextmenü einen eindeutigen Index, und man hat die Liste links als weiteren Zugriff)